// Iteration 1: Names and Input

let hacker1 = `Billy`;
let hacker2 = `Bob`;

const driverName = hacker1;
const navigatorName  = hacker2;


console.log(`The driver's name is ${driverName}.`);

console.log(`The navigator's name is ${navigatorName}.`);

// Iteration 2: Conditionals


if(hacker1 == driverName) {
    console.log(`Driver` + ' ' + hacker1 + ' ' + `Ready!`);
} else {
        console.log(`Can't go anywhere without a driver!.`);
}

if(hacker2 == navigatorName) {
    console.log(`Navigator` + ' ' + hacker2 + ' ' + `Let's Go!`);
 } else {
        console.log(`Can't navigate without a navigator!`);
}

if('hacker1' && 'hacker2') {
    console.log(hacker1 + ' ' + hacker2 + ' ' + 'READY!');
} else {
    console.log(`No Crew! Not Ready!`);
}
    


// Iteration 3: Loops
